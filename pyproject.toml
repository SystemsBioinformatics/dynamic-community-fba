[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "endPointFBA"
version = "0.0.1"
authors = [
  { name="Steven Wijnen", email="steven.wijnen@icloud.com" },
  { name="Francesco Moro", email="f.moro@vu.nl"}
]
description = "Python package for endPointFBA"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://github.com/StevenWijnen/endPointFBA"
"Bug Tracker" = "https://github.com/StevenWijnen/endPointFBA/issues"

[tool.pytest.ini_options]
pythonpath = "./code"
addopts = [
    "--import-mode=importlib",
]

